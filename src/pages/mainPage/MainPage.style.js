import styled from "styled-components";
import { vw } from "../../utils/units";

export const MainContainer = styled.div`
  width: ${vw(1920)};
  height: ${vw(1080)};
  flex-shrink: 0;
  background: #fdfdfd;
`;
